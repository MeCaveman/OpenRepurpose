# v0.7 progress

## Completed
- Packet 1: added a provider-neutral `TranscriptionProvider` contract and composition-time
  registry, validated cue/word timestamp models, and deterministic transcription cache identity.
- Added migration `0021_transcripts`, durable transcript/cue tables, and a SQLite repository with
  atomic cache reservation, generated-cue replacement, and persisted cue edits.
- Added contract, cache, migration, SQLite constraint, and edit-preservation tests.
- Architectural retrofit: added the optional provider-neutral `ExternalDownloader` contract,
  capability/availability/version reporting, structured failures, composition-time registry, and
  the bridge into the existing durable media-resolution boundary. No downloader implementation or
  dependency was added.
- Packet 2: added the cross-platform whisper.cpp bridge with configured/managed/packaged/PATH
  executable discovery, direct version probing, versioned managed installation and model layouts,
  validated argument-array invocation, JSON cue parsing, progress, cancellation, timeout, shutdown,
  bounded diagnostics, and temporary-output cleanup.
- Added focused coverage for Windows/Unicode/shell-looking paths, UTF-8 output, missing models,
  managed installation discovery, spawned-process progress, and cancellation.
- Packet 3: added a curated, content-addressed whisper.cpp model catalog and local model manager
  with explicit streaming downloads, live progress, disk-reserve preflight, SHA-1 verification,
  atomic installation, safe deletion, configurable storage, and failure cleanup.
- Added CSRF-protected model list/download/verify/delete HTTP endpoints; matching CLI list,
  download, verify, and delete commands; and a responsive model-management view linked from the
  workbench rail and Settings.
- Added model-manager unit/API/UI coverage plus a seven-viewport browser journey. The compact
  navigation now keeps the active route visible at 390 px and 412 px.
- Packet 4: added a shared transcript application service, chronological cue validation,
  optimistic revision checks, atomic whole-transcript edit persistence, and deterministic UTF-8
  SRT/WebVTT serialization.
- Added CSRF-protected transcript read/edit/export HTTP endpoints and matching `transcript show`
  and `transcript export` CLI commands.
- Added a media-scoped transcript editor with paged cue editing, inline timestamp errors,
  Ctrl/Cmd+Enter save, unsaved-change protection, SRT/VTT downloads, and the existing 1440 px
  workbench/inspector transition.
- Packet 5: added immutable caption-render transform snapshots, safe FFmpeg burn-in compilation,
  and durable UTF-8 SRT sidecars alongside transform derivatives.
- Packet 6: added typed transcription and caption workflow steps. A workflow snapshots its
  continuation into a durable transcription job, then resumes the same immutable plan after local
  inference; captions compile from that transcript revision into the existing transform pipeline.
- Added workflow cache reuse keyed by the existing source fingerprint/provider/model/options
  identity. Complete generated and user-edited transcripts are reused without running inference;
  an edit that lands during inference is preserved rather than overwritten.
- Packet 7: expanded regression coverage for UTF-8/non-English transcript fixtures, pre-start and
  live cancellation, missing and corrupt models, edit persistence, invalid cue/word timing,
  platform-neutral caption font fallback, and Windows/Linux path handling.

## Decisions
- Cache identity includes source-audio fingerprint, provider, model ID/version, language, and
  canonicalized output-affecting options. Cache reuse returns the original transcript rather than
  creating a duplicate.
- A transcript references exactly one media asset or transform derivative. Generated cues cannot
  replace a transcript once a user edit has been persisted.
- The provider contract accepts a managed local audio path and does not imply cloud upload;
  whisper.cpp remains a Packet 2 implementation behind this boundary.
- Source discovery supplies only a generic external-download locator. Downloader process details
  and configuration belong to future infrastructure adapters, remain separate from source-account
  configuration, and are omitted from the registry when disabled or unavailable.
- The existing rights check runs before external downloader invocation; resolution checkpointing,
  import, jobs, FFmpeg processing, scheduling, and cleanup remain unchanged.
- Managed whisper.cpp installs adopt a prepared official/build distribution directory atomically;
  acquisition and archive verification stay outside the bridge, so no Python, shell command, or
  silent download is required. Models use `<root>/<model-id>/<version>/ggml-model.bin` and remain
  user-initiated Packet 3 downloads.
- The provider requests whisper.cpp JSON sidecars and exposes segment cues only. It reports
  `wordTimestamps: false` rather than treating model tokens as linguistic words.
- Model catalog versions are pinned by the official whisper.cpp SHA-1 digest. Downloads come only
  from the published Hugging Face repository after an explicit user action and become installed
  only after checksum verification and an atomic rename.
- Model storage defaults under the local application data directory, can be overridden with
  `WHISPER_MODEL_DIR`, and preserves a 256 MiB safety reserve during preflight. Browser closure does
  not cancel a server-side transfer.
- The model manager extends the existing Routing Workbench / Workshop Ledger vocabulary without a
  design-system version bump; Base Multilingual is the recommended starting model.
- Transcript exports preserve cue order, allow standards-valid overlaps, require nondecreasing
  start times, normalize line endings to LF, and use fixed-width hour/minute/second/millisecond
  timestamps. SRT uses comma milliseconds; WebVTT uses the required `WEBVTT` header and period
  milliseconds.
- Whole-document saves use the transcript revision as an optimistic concurrency boundary. Modified
  cues discard stale word timing while untouched cues preserve any existing word timing.
- The transcript editor composes the existing Direction E media/workbench vocabulary, tokens, and
  primitives; OpenRepurpose Design System 2.0 remains unchanged.
- Caption steps retain transcript ID/revision and a generated SRT snapshot in normalized transform
  provenance, so later edits cannot silently change queued or cached derivatives.
- Workflow transcription uses the imported local media fingerprint as its source-audio identity at
  the current boundary. A future dedicated audio-extraction artifact can supply a narrower
  fingerprint without changing cache semantics.

## Verification
- `corepack pnpm typecheck` — pass
- `corepack pnpm lint` — pass
- `corepack pnpm format:check` — pass
- `corepack pnpm test` — pass (50 files, 304 tests; includes a real FFmpeg UTF-8 burn-in fixture)
- `corepack pnpm build` — pass
- `corepack pnpm test:e2e` — pass (15 Chromium tests; browser launch verified outside the sandbox)
- Transcript browser journey — pass at 390x844, 412x915, 768x1024, 1366x768, 1440x900,
  1920x1080, and 2560x1440; confirms 44 px compact controls and the 1440 px wide-workbench
  boundary.
- `impeccable detect --json ...` — pass (no design-system drift)
- `corepack pnpm typecheck` — pass
- `corepack pnpm test -- --run tests/unit/workflow.test.ts` — pass (50 files, 305 tests)
- `corepack pnpm lint` — pass
- `corepack pnpm format:check` — pass
- `corepack pnpm exec vitest run tests/unit/model-manager.test.ts tests/unit/whisper-cpp.test.ts tests/unit/transcription.test.ts tests/unit/ffmpeg-command-builder.test.ts` — pass (4 files, 43 tests)
- `corepack pnpm verify` — pass (typecheck, lint, format, unit tests, build, and browser tests)
- `corepack pnpm test:e2e` — pass (15 Chromium tests)

## Known issues
- No user-facing transcription generate/cancel flow or executable acquisition flow yet. Resumable
  model downloads across process restarts remain out of scope. Workflow transcription requires a
  discovered local whisper.cpp executable and an explicitly installed model.

## Next packet
- v0.8: Twitch/Kick/OBS source specialization, beginning with its approved first packet.

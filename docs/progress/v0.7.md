# v0.7 progress

## Completed
- Packet 1: added a provider-neutral `TranscriptionProvider` contract and composition-time
  registry, validated cue/word timestamp models, and deterministic transcription cache identity.
- Added migration `0021_transcripts`, durable transcript/cue tables, and a SQLite repository with
  atomic cache reservation, generated-cue replacement, and persisted cue edits.
- Added contract, cache, migration, SQLite constraint, and edit-preservation tests.

## Decisions
- Cache identity includes source-audio fingerprint, provider, model ID/version, language, and
  canonicalized output-affecting options. Cache reuse returns the original transcript rather than
  creating a duplicate.
- A transcript references exactly one media asset or transform derivative. Generated cues cannot
  replace a transcript once a user edit has been persisted.
- The provider contract accepts a managed local audio path and does not imply cloud upload;
  whisper.cpp remains a Packet 2 implementation behind this boundary.

## Verification
- `corepack pnpm typecheck` — pass
- `corepack pnpm lint` — pass
- `corepack pnpm format:check` — pass
- `corepack pnpm test` — pass (47 files, 273 tests)
- `corepack pnpm build` — pass

## Known issues
- No inference process bridge, model management, transcript editor/export, caption rendering, or
  workflow integration yet; these remain in their respective v0.7 packets.

## Next packet
- Packet 2: implement the whisper.cpp process bridge, including executable/model discovery,
  argument-array invocation, progress, cancellation, and output parsing.

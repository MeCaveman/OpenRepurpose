# v0.4 progress

## Completed

- Packet 1: accepted ADR 0006 for source identity/cursor/dedupe, observed versus completed state,
  workflow-version execution uniqueness, per-destination recovery, media ownership, retention,
  cleanup eligibility, crash recovery, and persistent history.
- Added the detection-only generic `SourceAdapter` contract, registry, validation, deterministic
  test adapter, and contract tests.
- Added migration `0011_source_domain` and matching Drizzle schema for source connections/items,
  immutable workflow execution snapshots, independent destination results, and media artifacts.
- Added database constraints/triggers that keep successful destination checkpoints terminal, block
  cleanup while required destinations are incomplete, prevent user-owned originals from entering
  cleanup/deleted state, and preserve artifact audit rows after managed bytes are removed.
- Added v0.3 upgrade coverage and source-domain persistence/recovery tests.
- Packet 2: added a server-owned, headless `SourcePollingRunner` with persisted due-time
  scheduling, cursor/last-poll/last-success/error state, one-sided jitter, bounded error backoff,
  and authorization-failure pausing.
- Added `SqliteSourcePollingRepository`: repeated observations retain one source item per stable
  external ID while refreshing its observation metadata/timestamp.
- Each durable observation enqueues one idempotent `source.item.observed` handoff job. The runner
  never resolves media, creates workflow executions, or publishes destinations directly.
- Added polling-runner tests for observation/handoff durability, restart dedupe, and repeated auth
  failure pausing.

## Decisions

- Cursor/watermark state is an optimization and advances only after observed items/executions are
  durable; `(source connection, stable external ID)` is the correctness boundary.
- Event IDs are audit data, not item identity. The unique execution intent is
  `(source item, workflow key, workflow version)` with an immutable snapshot.
- Existing jobs remain the retry/lease engine and existing destination job records remain adapter
  checkpoints; source execution destinations add orchestration-level result state, not a parallel
  publisher.
- `delete_after_success` is the downloaded/generated-media default; `keep_for_duration` requires a
  positive duration; `keep_forever` never enters automatic cleanup.
- Only `openrepurpose_temporary` and `openrepurpose_generated` artifacts can be cleanup candidates.
  `user_owned_original` artifacts are permanently protected.
- Source scheduling jitter only increases the configured interval, so a source is never polled
  sooner than its configured minimum. Cursor advancement follows successful durable item/handoff
  persistence; replay after a crash is safe because item and job idempotency keys dedupe it.

## Verification

- `corepack pnpm exec vitest run tests/unit/platform-sdk.test.ts tests/unit/source-domain.test.ts tests/unit/database.test.ts` — pass (17 tests).
- `corepack pnpm exec vitest run tests/unit/source-polling-runner.test.ts tests/unit/source-domain.test.ts tests/unit/platform-sdk.test.ts` — pass (15 tests).
- `corepack pnpm typecheck` — pass.
- `corepack pnpm lint` — pass.
- `corepack pnpm format:check` — pass.
- `corepack pnpm test` — pass (89 tests).
- `corepack pnpm build` — pass.
- `corepack pnpm test:e2e` — pass (6 tests).

## Known issues

- No platform adapter is registered yet; Packet 3 supplies YouTube detection. Pending
  `source.item.observed` jobs are intentional handoff records until Packet 5 implements workflow
  execution integration.
- Media resolution/storage cleanup and workflow execution integration remain intentionally deferred
  to their owning packets.
- Exactly-once effects cannot be guaranteed across remote APIs when a remote acceptance and local
  checkpoint commit are separated by a crash; adapters must reconcile persisted remote operation
  IDs/idempotency keys and surface ambiguity instead of blindly republishing.

## Next packet

- Packet 3: implement YouTube channel/upload detection with mocked API tests and quota-conscious
  detection/metadata calls, separate from media resolution.

# v0.5 progress

## Completed

- Packet 1: accepted ADR 0007 for persistent schedule/occurrence ownership, Croner-backed
  `cron-v1` recurrence, IANA timezone/DST behavior, coalesced restart catch-up, deterministic
  occurrence/job idempotency, and integration with the existing source poller and job runner.
- Packet 2: added Croner 10.0.1-backed `cron-v1` calculation, explicit IANA-zone one-time
  resolution, durable schedules/occurrences, schedule-owned source-poll cadence, a server-owned
  durable scheduler loop, and deterministic fake-clock coverage for restart coalescing and DST.
- Packet 3: added SQLite-backed global/platform/account claim limits, persisted queue
  pause/resume/drain state, platform/account Retry-After cooldowns, repeated auth/permission
  account pauses, manual failed-job retry, account resume, and protected HTTP control routes.
- Packet 3 preserves successful destination checkpoints when retrying a failed source-execution
  destination and adds restart coverage for queue/circuit state.
- Packet 4 added the `workflow-plan-v1` typed step DAG, structural graph validation, and a
  compiler that produces the sole execution-plan shape consumed by workflow execution.
- Packet 4 added migration `0017_workflow_execution_plans`, which backfills each existing workflow
  with a typed source/destination definition and versioned plan while retaining legacy columns as
  compatibility projections.
- Packet 5 added a dependency-free structured workflow editor with Source, optional Filter,
  optional Schedule, and one-or-more Destination stages. It submits the typed `workflow-plan-v1`
  definition through the existing workflow service, which remains authoritative for validation and
  compilation. Saved workflows now show a readable stage summary.
- Packet 6 added the planned CLI surfaces: `schedule list`, `schedule show`, `jobs retry`,
  `jobs cancel`, `jobs pause`, and `jobs resume`. Schedule reads use the shared `ScheduleService`
  and SQLite repository; job controls use the existing `JobService` and persisted queue state.
- Packet 7 added a deterministic fake-clock and mocked-adapter reliability suite covering restarts
  one second before and after due, DST gaps and overlaps, remote Retry-After, account auth circuit
  pause/resume, failed-destination-only manual retry, and simultaneous source-poll/publish work.
- Packet 7 added a populated v0.4 migration test that verifies source cadence defaults, new durable
  schedule/job-control tables, and remote workflow conversion to `workflow-plan-v1`.
- Post-release UI Phase 8 added the token-backed domain-pattern layer for platform identity,
  connections, workflow routes/cards/status, job status, and resource empty states. Existing pages
  consume only the repeated fragments; route structure and v0.5 behavior remain unchanged.
- UI Phase 9 migration group 1 added the token-backed application shell, command bar, workspace,
  page header, and keyboard skip link. Optional pane slots remain absent when unused, the inspector
  defaults closed, and the current routes, navigation targets, API calls, and page behavior remain
  unchanged.
- UI Phase 9 migration group 2 replaced the legacy sidebar markup with one token-backed global
  resource-navigation component: an icon-led rail at workbench widths and labeled horizontal
  navigation at compact/narrow widths. The existing routes and history behavior remain unchanged.
- UI Phase 9 migration group 3 extracted `/setup` presentation into `features/setup` and replaced
  raw-color readiness cards with shared panels, platform identity, semantic badges, inline alerts,
  and a stable local loading state. The setup API and credential semantics remain unchanged.
- UI Phase 9 migration group 4 extracted `/` presentation into `features/overview` and replaced the
  roadmap placeholder with a route-start ledger linking the existing Sources, Workflows, and
  Destinations views plus a local-first setup note. No API request or speculative status was added.
- UI Phase 9 migration group 5 extracted `/accounts` presentation into `features/accounts` and
  migrated credential forms, Meta identities/targets, connection results, capability details, and
  connected-account states to the shared primitive and domain-pattern layers. Existing account,
  OAuth, credential, capability, and target API contracts remain unchanged.
- UI Phase 9 migration group 6 extracted `/sources` presentation into `features/sources` and
  migrated YouTube source setup, polling controls/history, connection errors, and observed-item
  resolution/lifecycle/cleanup states to shared primitives and token-backed patterns. Existing
  source polling, pause/resume, lifecycle, ownership, and API semantics remain unchanged.
- UI Phase 9 migration group 7 extracted `/workflows` presentation and its structured editor into
  `features/workflows`. The route builder now uses progressive optional-stage disclosure, explicit
  page states, the shared route preview, and saved workflow cards while preserving definition,
  validation, execution-plan, persistence, and API semantics.
- UI Phase 9 migration group 8 extracted `/jobs` presentation into `features/jobs` and migrated the
  execution ledger, destination checkpoints, cancellation state, and contextual attempt history to
  shared primitives and token-backed patterns. Existing job, attempt, cancellation, persistence,
  queue, and API semantics remain unchanged.
- UI Phase 9 migration group 9 extracted `/settings` presentation into `features/settings` and
  replaced the future-tense placeholder with a configuration-ownership ledger plus fixed
  local-installation policy. It adds no settings request, editable preference, or inferred runtime
  status; existing configuration, persistence, deployment, and API behavior remain unchanged.
- UI Phase 9 migration group 10 extracted `/media` presentation into `features/media` and the
  existing unknown-route fallback into `features/not-found`. Local import, technical metadata,
  media state, YouTube/TikTok preparation, direct-publish requests, and recovery navigation now use
  the shared UI system while existing media, publishing, job, routing, persistence, and API
  semantics remain unchanged.

## Decisions

- Persist both schedules and immutable materialized occurrences; UTC due instants are authoritative
  and process-local timers are wake-up aids only.
- Use exactly pinned Croner 10 in Packet 2 as a five-field calculation/validation library, not as an
  in-memory executor. Recurring DST gaps are skipped and overlaps run once at the earlier instant.
- Catch up an overdue one-time schedule once. Coalesce multiple missed recurring instants into one
  recorded firing, then advance strictly beyond the recovery time.
- Dispatch through existing jobs with deterministic occurrence keys. The source poller remains the
  only source adapter caller, with one cadence owner per source connection and cooldown/pause taking
  precedence over scheduled poll requests.
- Schedule timing is persisted as UTC milliseconds; requested one-time wall time and IANA timezone
  are retained for audit. Repeated local times choose the earlier UTC instant and nonexistent local
  times are rejected.
- Destination job concurrency scope is snapshotted onto each job. Claims check SQLite running counts
  and control rows transactionally, so correctness does not depend on one in-memory worker process.
- Retry-After accepts delta-seconds and HTTP-date values. A response cooldown applies to both the
  affected platform and account; the later persisted deadline wins.
- Three auth/permission failures pause only the affected account by default. A successful scoped job
  clears the active account's failure count; a paused account requires explicit resume.
- Queue pause and drain stop new claims without cancelling work. Drain waits for the database-wide
  running count to reach zero, while normal server shutdown performs a non-persistent local drain.
- Manual retry is restricted to failed jobs, retains attempt history/checkpoints, and grants one new
  attempt. Source-execution retry continues to select failed destinations only.
- Workflow definitions are validated before compilation; plans allow only source, filter,
  pass-through transform, schedule, and destination steps. Persisted plan JSON is an audit snapshot:
  repositories recompile definitions on read and executors revalidate the typed plan, rather than
  treating stored JSON as executable instructions.
- Schedule-owned source connections are due only while a durable scheduled-poll request is pending;
  a completed scheduled poll with no next request cannot be immediately polled again.
- Croner remains the recurrence calculator, but calculated instants that do not match the expression
  in its IANA timezone are skipped so spring-forward gaps follow ADR 0007 instead of shifting later.
- The v0.5 workflow visualization remains a read-only source-to-stage-to-destination route, not a
  graphical editor. Provider display metadata is separate from generic connection/route patterns,
  and the design-system version remains 1.0 because Phase 8 adds no new visual vocabulary.
- The Phase 9 group 1 shell is border-led and token-backed. It exposes resource-rail, navigator,
  inspector, and activity regions without rendering them speculatively; existing global navigation
  remains in `App.tsx` until migration group 2. The design-system version remains 1.0.
- Phase 9 group 2 keeps route configuration and URL/history ownership in `App.tsx` while shared
  resource-navigation presentation lives in `components/layout`. No contextual navigator is shown
  because current v0.5 routes do not expose contextual collections. The design-system version
  remains 1.0.
- Phase 9 group 3 keeps the shared YouTube/TikTok credential state and `/api/setup` request in
  `App.tsx`, while `features/setup` owns normalized readiness presentation. Setup remains spacious,
  omits contextual panes, and exposes technical callback detail without letting platform identity
  dominate. The design-system version remains 1.0.
- Phase 9 group 4 treats overview as orientation rather than analytics: it uses existing routes and
  the product's source-to-workflow-to-destination vocabulary without inventing metrics or loading
  unrelated resource data. The design-system version remains 1.0.
- Phase 9 group 5 keeps account requests, mutations, OAuth redirects, capability discovery, and
  shared account state in `App.tsx`; `features/accounts` receives normalized view data and callbacks.
  Credential requests and target/account mutations now expose stable button progress, and local
  connection removal requires confirmation. No token or shared visual vocabulary change was needed,
  so the design-system version remains 1.0.
- Phase 9 group 6 keeps source requests, mutations, polling behavior, and shared source state in
  `App.tsx`; `features/sources` receives normalized view data and callbacks. `SourceStatus` extends
  the documented domain-pattern layer using existing semantic badges, while item resolution,
  lifecycle, and cleanup details remain feature-local until a second consumer justifies promotion.
  No new token or visual vocabulary was needed, so the design-system version remains 1.0.
- Phase 9 group 7 keeps workflow loading, creation requests, CSRF handling, and shared account/source
  state in `App.tsx`; `features/workflows` owns route-builder presentation, feature-local editor
  state, route normalization, and saved-route states. Optional filter, pass-through transform, and
  schedule controls are collapsed until requested, while the route preview stays read-only. No new
  token or shared visual vocabulary was needed, so the design-system version remains 1.0.
- Phase 9 group 8 keeps job list/detail requests, CSRF handling, and cancellation mutations in
  `App.tsx`; `features/jobs` owns normalized job/attempt view data, responsive ledger/detail
  presentation, confirmation, and request-progress states. Attempt detail opens only after
  selection and becomes the sole primary pane below the wide layout. The activity shelf remains
  absent because v0.5 has no separate live activity stream. No new token or shared visual
  vocabulary was needed, so the design-system version remains 1.0.
- Phase 9 group 9 treats settings as a map of existing configuration ownership rather than a wall
  of nonfunctional controls. `features/settings` links only to current setup, account, and workflow
  routes and records local-first product boundaries as policy, not live system state. The spacious
  setup workspace mode is reused, optional panes remain absent, and no new token or shared visual
  vocabulary was needed, so the design-system version remains 1.0.
- Phase 9 group 10 keeps media/account loading, CSRF handling, import, and direct-publish mutations
  in `App.tsx`; `features/media` owns normalized file-ledger and publish-preparation presentation.
  Publish preparation remains closed until selection, shares context with the ledger at wide
  widths, and becomes the sole primary pane at smaller widths. The existing fallback route receives
  one semantic Dashboard recovery link. No new token or shared visual vocabulary was needed, so the
  design-system version remains 1.0.

## Verification

- `corepack pnpm verify` — typecheck, lint, format check, 114 unit/integration tests, and build
  passed; the sandboxed Playwright phase could not launch Chromium (`spawn EPERM`).
- `corepack pnpm test:e2e` — pass outside the browser-launch sandbox (6 tests).
- `git diff --check` — pass.
- `corepack pnpm test:e2e` — blocked in this sandbox because Chromium launch fails with
  `spawn EPERM`; the six existing browser tests did not reach application assertions.
- `corepack pnpm test` — pass (126 tests).
- `corepack pnpm typecheck` — pass.
- `corepack pnpm lint` — pass.
- `corepack pnpm format:check` — pass.
- `corepack pnpm build` — pass.
- `corepack pnpm test:e2e` — blocked by sandboxed Chromium launch (`spawn EPERM`); the existing
  browser suite was not changed.
- Focused Packet 4 suite (`workflow-plan`, `workflow`, and `database`) — pass (126 tests).
- `corepack pnpm verify` — typecheck, lint, format check, 126 unit/integration tests, and build
  passed; its sandboxed Playwright phase could not launch Chromium (`spawn EPERM`).
- `corepack pnpm test:e2e` — pass outside the browser-launch sandbox (6 tests).
- `corepack pnpm typecheck` — pass.
- `corepack pnpm lint` — pass.
- `corepack pnpm format:check` — pass.
- `corepack pnpm test` — pass (116 tests).
- `corepack pnpm build` — pass.
- Focused Packet 3 suite — pass (41 tests covering jobs, migrations, configuration, Retry-After,
  restart recovery, and failed-destination-only recovery).
- Focused Packet 6 suite — pass (`tests/unit/jobs.test.ts` and `tests/unit/scheduling.test.ts`,
  17 tests).
- `corepack pnpm --filter @openrepurpose/cli typecheck` — pass.
- `git diff --check` — pass.
- `corepack pnpm verify` — typecheck, lint, format check, 122 unit/integration tests, and build
  passed; the sandboxed Playwright phase could not launch Chromium (`spawn EPERM`).
- `corepack pnpm test:e2e` — pass outside the browser-launch sandbox (6 tests).
- `git diff --check` — pass.
- Packet 7 focused reliability/migration suite — pass (36 tests).
- Populated v0.4 migration test — pass.
- `corepack pnpm verify` — typecheck, lint, format check, 137 unit/integration tests, and build
  passed; its sandboxed Playwright phase could not launch Chromium (`spawn EPERM`).
- `corepack pnpm test:e2e` — pass with approved browser launch (6 tests).
- `git diff --check` — pass.
- UI Phase 8 focused Vitest suites — pass (16 tests).
- UI Phase 8 `corepack pnpm test:e2e` — pass with approved browser launch (6 tests).
- UI Phase 8 live visual check — workflow branching verified at 1280px and vertical route reflow
  verified at the 320px minimum width.
- UI Phase 9 group 1 `corepack pnpm typecheck` — pass.
- UI Phase 9 group 1 `corepack pnpm lint` — pass.
- UI Phase 9 group 1 `corepack pnpm format:check` — pass.
- UI Phase 9 group 1 `corepack pnpm test` — pass (158 tests).
- UI Phase 9 group 1 `corepack pnpm build` — pass.
- UI Phase 9 group 1 focused layout suite — pass (5 tests).
- UI Phase 9 group 1 `corepack pnpm test:e2e` — pass outside the browser-launch sandbox (7 tests),
  including desktop/320px shell reflow, no document overflow, and keyboard skip-link coverage.
- UI Phase 9 group 2 `corepack pnpm typecheck` — pass.
- UI Phase 9 group 2 `corepack pnpm lint` — pass.
- UI Phase 9 group 2 `corepack pnpm format:check` — pass.
- UI Phase 9 group 2 `corepack pnpm test` — pass (160 tests).
- UI Phase 9 group 2 `corepack pnpm build` — pass.
- UI Phase 9 group 2 focused layout suite — pass (7 tests).
- UI Phase 9 group 2 `corepack pnpm test:e2e` — pass outside the browser-launch sandbox (7 tests),
  including desktop resource rail, narrow/compact navigation, keyboard skip-link, route selection,
  and 320px overflow coverage.
- UI Phase 9 group 2 visual smoke — desktop (1440px) and compact (320px) screenshots reviewed;
  navigation hierarchy, active state, spacing, and overflow are correct.
- UI Phase 9 group 3 `corepack pnpm typecheck` — pass.
- UI Phase 9 group 3 `corepack pnpm lint` — pass.
- UI Phase 9 group 3 `corepack pnpm format:check` — pass.
- UI Phase 9 group 3 `corepack pnpm test` — pass (164 tests).
- UI Phase 9 group 3 `corepack pnpm build` — pass.
- UI Phase 9 group 3 focused setup suite — pass (4 tests covering configured, action-required,
  loading, and error states).
- UI Phase 9 group 3 `corepack pnpm test:e2e` — pass outside the browser-launch sandbox (7 tests),
  including setup readiness, callback display, platform limitation, and 320px overflow coverage.
- UI Phase 9 group 3 visual smoke — configured setup reviewed at 1440px and 320px; hierarchy,
  callback wrapping, status legibility, limitation treatment, and spacing are correct.
- UI Phase 9 group 4 `corepack pnpm typecheck` — pass.
- UI Phase 9 group 4 `corepack pnpm lint` — pass.
- UI Phase 9 group 4 `corepack pnpm format:check` — pass.
- UI Phase 9 group 4 `corepack pnpm test` — pass (167 tests).
- UI Phase 9 group 4 `corepack pnpm build` — pass.
- UI Phase 9 group 4 focused overview suite — pass (3 tests).
- UI Phase 9 group 4 `corepack pnpm test:e2e` — pass outside the browser-launch sandbox (8 tests),
  including ordered overview links, route navigation, and 320px overflow coverage.
- UI Phase 9 group 4 visual smoke — overview reviewed at 1440px and 320px; ledger hierarchy,
  route order, text wrapping, navigation affordances, and spacing are correct.
- UI Phase 9 group 5 `corepack pnpm typecheck` — pass.
- UI Phase 9 group 5 `corepack pnpm lint` — pass.
- UI Phase 9 group 5 `corepack pnpm format:check` — pass.
- UI Phase 9 group 5 `corepack pnpm test` — pass (171 tests).
- UI Phase 9 group 5 `corepack pnpm build` — pass.
- UI Phase 9 group 5 focused accounts suite — pass (4 tests covering provider forms, Meta targets,
  connection feedback/capabilities, action progress, and the empty state).
- UI Phase 9 group 5 `corepack pnpm test:e2e` — pass outside the browser-launch sandbox (8 tests),
  including Meta target PATCH behavior, distinct Facebook/Instagram targets, and 320px overflow
  coverage.
- UI Phase 9 group 5 visual smoke — accounts reviewed at 1280px and 320px; configuration hierarchy,
  callback wrapping, target/status legibility, platform restraint, and spacing are correct.
- UI Phase 9 group 6 `corepack pnpm typecheck` — pass.
- UI Phase 9 group 6 `corepack pnpm lint` — pass.
- UI Phase 9 group 6 `corepack pnpm format:check` — pass.
- UI Phase 9 group 6 `corepack pnpm test` — pass (175 tests).
- UI Phase 9 group 6 `corepack pnpm build` — pass.
- UI Phase 9 group 6 focused source/pattern suites — pass (15 tests covering source setup,
  connection and item states, loading, errors, action progress, and recovery/empty states).
- UI Phase 9 group 6 `corepack pnpm test:e2e` — pass outside the browser-launch sandbox (9 tests),
  including the source poll POST, observed-media lifecycle detail, source controls, and 320px
  overflow coverage.
- UI Phase 9 group 6 visual smoke — sources reviewed at 1280px and 320px; setup hierarchy, polling
  state/history, lifecycle badge reflow, ownership warning, actions, and spacing are correct.
- UI Phase 9 group 7 `corepack pnpm typecheck` — pass.
- UI Phase 9 group 7 `corepack pnpm lint` — pass.
- UI Phase 9 group 7 `corepack pnpm format` / `corepack pnpm format:check` — pass.
- UI Phase 9 group 7 `corepack pnpm test` — pass (179 tests).
- UI Phase 9 group 7 `corepack pnpm build` — pass.
- UI Phase 9 group 7 focused workflow page/pattern suites — pass (15 tests covering the editor,
  progressive controls, saved route stages, loading/error/empty states, and active-source options).
- UI Phase 9 group 7 `corepack pnpm test:e2e` — pass outside the browser-launch sandbox (10 tests),
  including exact workflow payload/edge preservation, saved-route refresh, optional-stage default,
  and 320px overflow coverage.
- UI Phase 9 group 7 browser smoke — workflow builder exercised in Chromium at desktop and 320px;
  form submission, route refresh, progressive controls, focusable semantics, and reflow are correct.
- UI Phase 9 group 8 `corepack pnpm typecheck` — pass.
- UI Phase 9 group 8 `corepack pnpm lint` — pass.
- UI Phase 9 group 8 `corepack pnpm format` / `corepack pnpm format:check` — pass.
- UI Phase 9 group 8 `corepack pnpm test` — pass (183 tests).
- UI Phase 9 group 8 `corepack pnpm build` — pass.
- UI Phase 9 group 8 focused job page/pattern suites — pass (15 tests covering persisted status,
  destination checkpoints, attempts, loading/error/empty states, and cancellation progress).
- UI Phase 9 group 8 `corepack pnpm test:e2e` — pass outside the browser-launch sandbox (10 tests),
  including attempt selection/return, cancellation confirmation and POST behavior, persisted
  cancellation state, destination processing detail, and 320px single-pane overflow coverage.
- UI Phase 9 group 8 browser smoke — jobs exercised in Chromium at desktop and 320px; ledger/detail
  hierarchy, table-to-list adaptation, compact single-pane transition, actions, and wrapping are
  correct.
- UI Phase 9 group 9 `corepack pnpm typecheck` — pass.
- UI Phase 9 group 9 `corepack pnpm lint` — pass.
- UI Phase 9 group 9 `corepack pnpm format` / `corepack pnpm format:check` — pass.
- UI Phase 9 group 9 `corepack pnpm test` — pass (186 tests).
- UI Phase 9 group 9 `corepack pnpm build` — pass.
- UI Phase 9 group 9 focused settings suite — pass (3 tests covering route ownership, local
  installation policy, unsupported-control boundaries, and token usage).
- UI Phase 9 group 9 `corepack pnpm test:e2e` — pass outside the browser-launch sandbox (11 tests),
  including exact settings links, absence of invented controls, and 320px overflow coverage.
- UI Phase 9 group 9 browser smoke — settings reviewed in Chromium at 1440px and 320px;
  configuration hierarchy, policy separation, compact reflow, navigation affordances, and spacing
  are correct.
- UI Phase 9 group 10 `corepack pnpm typecheck` — pass.
- UI Phase 9 group 10 `corepack pnpm lint` — pass.
- UI Phase 9 group 10 `corepack pnpm format` / `corepack pnpm format:check` — pass.
- UI Phase 9 group 10 `corepack pnpm test` — pass (193 tests).
- UI Phase 9 group 10 `corepack pnpm build` — pass.
- UI Phase 9 group 10 focused media/fallback suites — pass (7 tests covering import, persisted
  media, loading/error/empty states, YouTube/TikTok preparation, token usage, and route recovery).
- UI Phase 9 group 10 `corepack pnpm test:e2e` — pass outside the browser-launch sandbox (12 tests),
  including exact YouTube publish payload preservation, fallback navigation, 1920px and 320px
  overflow coverage, and compact single-pane behavior.
- UI Phase 9 group 10 browser smoke — media reviewed in Chromium at 1280px, 1920px, and 320px;
  import/library hierarchy, technical metadata, contextual publishing, compact reflow, controls,
  and spacing are correct.

## Known issues

- Schedule creation remains outside the CLI/HTTP/UI surface; Packet 6 provides inspection only and
  keeps schedule timing/timezone ownership with the durable scheduler.
- Queue controls do not yet have a dedicated web UI; Packet 5 owns workflow UI work and later UX can
  consume the Packet 3 HTTP/application surfaces.
- Transform behavior remains a pass-through placeholder until v0.6; the Packet 5 editor exposes
  that boundary without implementing media operations.
- Schedule creation/listing and resolved next-run display remain outside the current HTTP/UI
  surface; the editor accepts an existing schedule ID and clearly leaves timing/timezone ownership
  with the durable scheduler.

## Next packet

- Phase 9 migration acceptance: review the completed migration across all current v0.5 routes and
  define the next explicitly approved phase. Do not begin v0.6 feature work without that approval.

# v0.5 progress

## Completed

- Packet 1: accepted ADR 0007 for persistent schedule/occurrence ownership, Croner-backed
  `cron-v1` recurrence, IANA timezone/DST behavior, coalesced restart catch-up, deterministic
  occurrence/job idempotency, and integration with the existing source poller and job runner.

## Decisions

- Persist both schedules and immutable materialized occurrences; UTC due instants are authoritative
  and process-local timers are wake-up aids only.
- Use exactly pinned Croner 10 in Packet 2 as a five-field calculation/validation library, not as an
  in-memory executor. Recurring DST gaps are skipped and overlaps run once at the earlier instant.
- Catch up an overdue one-time schedule once. Coalesce multiple missed recurring instants into one
  recorded firing, then advance strictly beyond the recovery time.
- Dispatch through existing jobs with deterministic occurrence keys. The source poller remains the
  only source adapter caller, with one cadence owner per source connection and cooldown/pause taking
  precedence over scheduled poll requests.

## Verification

- `corepack pnpm verify` — typecheck, lint, format check, 114 unit/integration tests, and build
  passed; the sandboxed Playwright phase could not launch Chromium (`spawn EPERM`).
- `corepack pnpm test:e2e` — pass outside the browser-launch sandbox (6 tests).
- `git diff --check` — pass.

## Known issues

- Scheduler schema/service code and the Croner dependency are intentionally deferred to Packet 2.
- The v0.4 source connection model has only interval cadence; Packet 2 must add the minimal persisted
  cadence/request distinction without changing existing connections' default behavior.

## Next packet

- Packet 2: implement schedule persistence/service, next-occurrence calculation, clock abstraction,
  scheduler loop, source-poller cadence compatibility, and deterministic fake-clock tests.

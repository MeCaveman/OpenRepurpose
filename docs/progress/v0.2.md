# v0.2 progress

## Completed
- Packet 1: verified current official TikTok Login Kit and Content Posting API documentation and
  added ADR 0004 for the Direct Post boundary.
- Packet 2: added the TikTok OAuth/account integration with persistent one-time state, desktop
  PKCE or HTTPS web flow selection, encrypted token-bundle storage/rotation, identity lookup,
  live creator-info capabilities, mocked service/HTTP tests, setup UI, and BYO app documentation.
- Packet 3: added the TikTok Direct Post transport: creator preflight validation, official
  `FILE_UPLOAD` initialization, sequential local-file stream chunks, durable publish-ID status
  polling, and local job-runner registration.
- Packet 4: added durable ordered workflow destinations, migrated existing YouTube workflows,
  and changed workflow execution to enqueue independent idempotent YouTube/TikTok destination
  jobs with persisted per-job destination checkpoints and best-effort failure isolation.
- Added migration `0007_tiktok_oauth` and a populated-v0.1 upgrade test while preserving existing
  YouTube accounts and workflow references.
- Added migration `0008_workflow_destinations` plus fan-out persistence, idempotency, mixed-outcome,
  and restart coverage.
- Packet 5: added the TikTok manual-publish API and CLI commands, capability-driven TikTok metadata
  controls in the media UI, destination limitation/status indicators, and durable destination
  checkpoint details in job API/UI output. Workflow HTTP input now accepts Packet 4 destination
  configurations.
- Packet 6: added a mocked release integration suite covering TikTok private/test-mode restriction,
  unsupported-format rejection before post creation, resumable upload retry without a second init,
  remote processing failure after local upload completion, and one best-effort workflow execution
  where YouTube succeeds while TikTok fails independently.

## Decisions
- v0.2 supports only Direct Post video uploads from local files (`FILE_UPLOAD`); no URL pulls,
  drafts, photos, scraping, browser automation, or audit workaround.
- Creator info is live, per-account preflight data rather than a static destination capability.
  Existing static `DestinationCapabilities` types remain unchanged; Packet 2 will add a
  TikTok-specific browser-safe preflight DTO.
- OAuth must choose a configured desktop loopback + required PKCE flow or HTTPS web/VPS flow.
  Tokens, PKCE verifiers, and sensitive upload URLs stay behind `SecretStore`.
- TikTok access and refresh tokens plus expirations/scopes are stored as one `SecretStore` bundle
  so a rotated refresh token replaces the previous bundle in one write; SQLite stores only safe
  identity and derived capability flags.
- TikTok does not expose developer-app audit status through creator-info. The UI distinguishes a
  creator offering public visibility from confirmed public-post availability and always surfaces
  the unaudited-client private-only restriction.
- A Direct Post `publish_id`, local transfer progress, and remote status are checkpointed in
  SQLite. The expiring signed upload URL is stored only as an encrypted SecretStore lease and is
  removed after transfer; recovery without a valid lease stops visibly rather than creating a
  second Direct Post.
- Chunk retries begin at the last confirmed 206 boundary. A 5xx/network failure never triggers a
  second init request; the normal job retry policy supplies bounded backoff and honors Retry-After
  for TikTok API rate limits.
- A workflow stores one configuration per destination and uses only `best_effort`: every
  destination receives its own durable job and idempotency key, so one terminal failure neither
  rolls back nor overwrites another destination's success. `all_or_nothing` is intentionally not
  supported because remote publishes cannot be safely rolled back.
- The v0.1 workflow input remains accepted and is normalized to one YouTube destination; existing
  persisted workflows are migrated the same way.
- UI and CLI validate TikTok privacy against the live creator-info capability response before
  queueing; the job handler remains the final validation boundary for restart-safe execution.

## Verification
- Documentation links checked against current official TikTok developer documentation on
  2026-09-17 and OAuth/token/creator-info behavior rechecked on 2026-09-18. Direct Post,
  media-transfer, and status-polling behavior were rechecked on 2026-09-18.
- `corepack pnpm format:check` — pass.
- `corepack pnpm typecheck` — pass.
- `corepack pnpm lint` — pass.
- `corepack pnpm test` — pass (15 files, 59 tests).
- `corepack pnpm build` — pass.
- `corepack pnpm test:e2e` — pass (5 Chromium journeys; required local browser-launch
  permission on this host; the sandboxed attempt failed with `spawn EPERM` before the permitted
  rerun passed).
- Packet 5 checks: `corepack pnpm format:check`, `corepack pnpm typecheck`, `corepack pnpm test`
  (15 files, 59 tests), `corepack pnpm lint`, and `corepack pnpm build` — pass.
- Packet 6 focused checks: `corepack pnpm exec vitest run tests/integration/v02-release.test.ts
  tests/unit/database.test.ts` — pass (2 files, 8 tests), including the populated v0.1-to-current
  migration fixture.
- v0.2 acceptance gate: `corepack pnpm format:check`, `corepack pnpm typecheck`,
  `corepack pnpm lint`, `corepack pnpm test`, and `corepack pnpm build` — pass (16 files, 64 tests).
  `corepack pnpm test:e2e` — pass (5 Chromium journeys; the sandboxed browser launch first failed
  with `spawn EPERM`, then the permitted local rerun passed).

## Known issues
- TikTok unaudited clients are restricted to private viewing; a TikTok audit is an external
  requirement and its status is not exposed by creator-info.
- TikTok does not document a safe status/resume operation for an incomplete `FILE_UPLOAD` after
  its signed upload URL expires. OpenRepurpose preserves the existing `publish_id` and reports a
  recovery ambiguity instead of duplicating a post; a future user-resolution flow can be designed
  only with an official safe recovery mechanism.
- Multi-destination workflow configuration and per-destination status are available in the core
  and persistence layers; workflow editing remains API-only in this packet.
- Release tests use mocked platform HTTP and do not replace optional live API smoke tests, which
  remain credentialed and opt-in by design.

## Next packet
- Packet 7: begin only after v0.2 Packet 6 review/commit; do not start a later version or Meta
  integration in this packet.

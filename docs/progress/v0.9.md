# v0.9 progress

## Completed
- Packet 1: added ADR 0012, a release-gating threat model for REST, MCP, webhooks, and opt-in
  LAN/headless control surfaces.
- Covered malicious localhost pages and DNS rebinding, LAN exposure, bearer leakage, MCP authority,
  command injection, path traversal, webhook SSRF, untrusted workflow input, and secret redaction.
- Packet 2: formalized `/api/v1` for health/version, safe accounts, media/import, sources,
  workflows, jobs/cancellation, schedules, transforms, transcripts, publishing, and token
  administration while preserving the legacy browser routes.
- Added an OpenAPI 3.1 document generated from the enforced strict Zod request contracts, stable
  error envelopes, bounded cursor pagination, and durable idempotency for create/import/publish
  operations.
- Added read/control API tokens with one-time disclosure, verifier-only SQLite persistence,
  header-only authentication, immediate revocation, and browser session/Origin/CSRF separation.
- Added migration `0025_api_v1`, v0.8 upgrade coverage, response DTO redaction for local paths and
  resumable upload URLs, and API boundary/security regression tests. No frontend code changed.
- Packet 3: added a dependency-free MCP 2025-03-26 stdio JSON-RPC server with the ten allowlisted
  tools: status, safe account/media/workflow/job reads, workflow execution, publishing, remote
  workflow scheduling, and cancellation.
- Added strict Zod schemas and generated tool input schemas, read-only/destructive annotations,
  safe response projections that omit local paths, media fingerprints, job inputs, and attempt
  error messages, plus protocol/schema/stdio tests. No frontend code changed.
- Packet 4: added outbound subscriptions for job success/failure and workflow execution
  start/completion, with safe versioned payloads and HMAC-SHA256 timestamp/delivery signatures.
- Added explicit environment-configured destination allowlisting, `SecretStore` signing keys,
  durable delivery records, shared-job-runner retries/attempt history, and crash-window recovery.
- Added per-attempt DNS resolution and address pinning, connected-peer revalidation, disabled
  redirects, metadata/link-local/reserved target blocking, bounded response bodies/timeouts, and
  SSRF/rebinding regression tests. No frontend code changed.
- Packet 5: enabled explicit authenticated LAN/headless operation. Loopback remains the default;
  a non-loopback bind or public `APP_URL` now fails closed unless `LAN_ENABLED=true` and a
  32-character minimum browser access token are configured.
- Added optional native TLS certificate/key loading, explicit reverse-proxy trust, a structured
  plaintext-LAN warning, HTTP Basic protection for every non-health browser/static route, and
  retained API-token permission checks for REST calls.
- Added `openrepurpose start --headless`, local API-token create/list/revoke administration,
  redacted configuration inspection, and an SQLite-backed stdio MCP launch command. Added Linux
  systemd, Windows Task Scheduler, TLS, reverse-proxy, and firewall guidance in
  `docs/self-hosting.md`. No frontend code changed.
- Packet 6: added an optional multi-stage Docker image based on Node 24 slim with Debian FFmpeg,
  an unprivileged runtime user, and only production server dependencies. Added a Compose example
  with required operator-supplied LAN authentication, host-local port binding, persistent config,
  data, model, and temporary-file volumes, plus an explicit read-only `/media` mount. Documented
  FFmpeg inclusion, whisper.cpp/model volume behavior, credential handling, backup guidance, and
  intentional media permissions. No frontend code changed.
- Packet 7: added an API/MCP integration E2E suite covering unauthenticated and revoked API tokens,
  cross-origin browser mutation rejection, duplicate idempotency replay before and after server
  restart, malicious REST/MCP path-like IDs, active job cancellation, MCP publishing through a mock
  destination handler, and metadata/link-local webhook rejection before persistence.
- Verified a populated v0.8 database upgrades through the v0.9 API and webhook migrations while
  preserving media, jobs, cancellation state, API credentials, and idempotency records. No frontend
  or production code changed.

## Decisions
- ADR 0012 extends the existing local HTTP protections instead of replacing them; loopback remains
  the default and browser session/Origin/CSRF authority stays separate from machine credentials.
- API/MCP credentials are revocable, verifier-only at rest, header-only, and distinguish read from
  control authority. MCP remains an allowlisted application-service facade with no raw
  shell/filesystem/SQL/secret/FFmpeg surface.
- Outbound webhooks are default-deny and require an explicit endpoint allowlist entry for public,
  private, or loopback targets. Link-local, metadata, unspecified, multicast, and reserved targets
  remain blocked, with DNS/peer validation on every delivery.
- Packet 1 changed documentation only; each later packet owns implementation and regression
  evidence for its release-gating controls.
- `/api/v1` delegates to the existing application services; transport code validates and maps DTOs
  but does not duplicate media, job, workflow, source, schedule, transform, or transcript logic.
- Token creation is not response-cached because persisting its one-time plaintext result would
  violate verifier-only storage. Other replay-sensitive creation paths require `Idempotency-Key`.
- Persisted `pending` idempotency records fail closed after an interrupted request instead of
  risking a duplicate side effect.
- MCP remains an exported stdio transport and service facade; its later CLI launch command and
  LAN/headless authentication configuration remain owned by Packet 5. The current tool set has no
  shell, filesystem, secret, SQL, URL-fetch, or arbitrary FFmpeg capability.
- `schedule_workflow` intentionally maps only remote-source workflows to the existing durable
  source-poll scheduler; local watched-folder workflows do not acquire a speculative scheduler.
- Webhook configuration is default-deny: every public, private, or loopback endpoint must appear in
  `WEBHOOK_DESTINATIONS_JSON`; removing it disables the persisted destination and deletes its
  signing key. Source/workflow data never supplies a URL.
- Webhook delivery reuses `JobRunner` for bounded concurrency, retry timing, attempts, and restart
  behavior. A separate webhook delivery row stores only the safe immutable event envelope and
  classified HTTP outcome; it stores no signing secret or response body.
- `workflow.execution.completed` means the immutable workflow dispatch finished and its jobs were
  accepted, not that remote publishing completed. Terminal destination outcomes use the job events.
- Docker keeps the existing LAN guard intact: because a container must listen on `0.0.0.0`, Compose
  requires `LAN_ACCESS_TOKEN` at startup even though the published host port is loopback-only. The
  image contains FFmpeg/ffprobe but not whisper.cpp or model data; models remain explicit persistent
  volume content.
- Packet 7 uses Fastify injection, the real SQLite repositories/migrations, MCP facade, shared job
  runner, and webhook network policy in one integration suite. This keeps the acceptance evidence at
  the application boundaries without adding a second browser-only or test-only execution path.

## Verification
- `corepack pnpm exec vitest run tests/unit/server.test.ts tests/unit/server-startup.test.ts tests/unit/platform-sdk.test.ts tests/unit/media-resolution.test.ts tests/unit/ffmpeg-command-builder.test.ts` — pass (5 files, 49 tests).
- `corepack pnpm verify` — pass (typecheck, lint, format check, 54 test files / 328 tests,
  build, and 16 Playwright tests). Playwright required the approved browser-launch permission after
  the sandbox correctly rejected Chromium with `spawn EPERM`.
- `node_modules/.bin/vitest run tests/unit/api-v1.test.ts tests/unit/server.test.ts tests/unit/database.test.ts tests/unit/transcription.test.ts tests/unit/jobs.test.ts tests/unit/scheduling.test.ts` — pass (6 files, 45 tests).
- `corepack pnpm typecheck && corepack pnpm lint && corepack pnpm format:check && corepack pnpm test && corepack pnpm build` — pass (55 test files, 333 tests).
- `corepack pnpm test:e2e` — pass (16 Playwright tests) with the approved browser-launch permission;
  the in-sandbox `corepack pnpm verify` reached E2E after all earlier stages passed, then Chromium
  was correctly blocked with `spawn EPERM`.
- `node_modules/.bin/vitest run tests/unit/mcp.test.ts tests/unit/api-v1.test.ts tests/unit/server.test.ts` — pass (3 files, 14 tests).
- `corepack pnpm typecheck && corepack pnpm lint && corepack pnpm format:check && corepack pnpm test && corepack pnpm build` — pass (56 files, 337 tests).
- `corepack pnpm test:e2e` — pass (16 Playwright tests).
- `node_modules/.bin/vitest run tests/unit/config.test.ts tests/unit/server-startup.test.ts tests/unit/server.test.ts tests/unit/mcp.test.ts` — pass (4 files, 23 tests).
- `node_modules/.bin/eslint . --max-warnings=0` — pass.
- `node_modules/.bin/prettier --check .` — pass.
- `node_modules/.bin/tsc -b` — pass.
- `corepack pnpm --offline build` — pass.
- `node_modules/.bin/vitest run` — pass (57 files, 347 tests).
- `corepack pnpm --offline test:e2e` — pass (16 Playwright tests). The initial sandboxed
  attempt correctly received `spawn EPERM`; the approved browser-launch rerun passed.
- `node_modules/.bin/vitest run tests/unit/webhooks.test.ts tests/unit/config.test.ts tests/unit/database.test.ts tests/unit/workflow.test.ts tests/unit/jobs.test.ts tests/unit/server-startup.test.ts` — pass (6 files, 49 tests).
- `corepack pnpm typecheck` — pass.
- `corepack pnpm lint` — pass.
- `corepack pnpm format:check` — pass.
- `corepack pnpm test` — pass (57 files, 345 tests). The first run found one stale migration-ledger
  expectation; it was updated for migration `0026_webhooks` and the complete rerun passed.
- `corepack pnpm build` — pass.
- `corepack pnpm test:e2e` — pass (16 Playwright tests).
- `corepack pnpm exec vitest run tests/unit/docker.test.ts` — pass (2 tests).
- `corepack pnpm typecheck` — pass.
- `corepack pnpm lint` — pass.
- `corepack pnpm format:check` — pass.
- `corepack pnpm test` — pass (58 files, 349 tests).
- `corepack pnpm build` — pass.
- `corepack pnpm test:e2e` — pass (16 Playwright tests).
- `docker compose config --no-interpolate` — pass; confirmed the four named volumes, read-only
  media bind, loopback port binding, healthcheck, and required token interpolation.
- `docker build --tag openrepurpose:packet-6 .` — not run to completion; Docker Desktop’s Linux
  engine was unavailable in the environment.
- `corepack pnpm exec vitest run tests/integration/v09-api-mcp-e2e.test.ts tests/unit/api-v1.test.ts tests/unit/mcp.test.ts tests/unit/webhooks.test.ts tests/integration/restart-recovery.test.ts tests/unit/database.test.ts` — pass (6 files, 28 tests).
- `corepack pnpm verify` — typecheck, lint, format check, 59 test files / 352 tests, and build pass;
  the in-sandbox Playwright stage was blocked from launching Chromium with `spawn EPERM`.
- `corepack pnpm test:e2e` — pass (16 Playwright tests) with the approved browser-launch permission.

## Known issues
- The Compose example does not package whisper.cpp; deployments that need transcription must supply
  a compatible executable through the existing discovery/configuration path and download models into
  the model volume explicitly.
- Webhook destinations remain startup environment configuration in v0.9. Delivery history is
  available through the application and repository boundary plus the shared job-attempt ledger; no
  frontend surface was added.
- An idempotency record interrupted while `pending` requires operator recovery; it intentionally
  remains fail-closed rather than expiring into a possible duplicate side effect.
- LAN browser access uses a startup environment secret so it can protect static assets before the
  application session exists. Operators should use HTTPS or a firewall-protected local reverse
  proxy; only health endpoints are intentionally unauthenticated for health checks.

## Next packet
- None in v0.9. Packet 7 completes the v0.9 roadmap; v1.0 work is a separate version boundary.

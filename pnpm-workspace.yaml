packages:
  - apps/*
  - packages/*
  - integrations/*

# Required so pnpm's non-legacy deploy can follow the complete workspace runtime graph.
injectWorkspacePackages: true

allowBuilds:
  esbuild: true

minimumReleaseAgeExclude:
  - fastify@5.12.5

saveExact: true
